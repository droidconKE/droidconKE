/**
 * Tracks a collection of stats.
 *
 * @constructor
 */
export declare class StatsCollection {
    private counters_;
    incrementCounter(description: string, amount?: number): void;
    get(): {
        [k: string]: number;
    };
}
